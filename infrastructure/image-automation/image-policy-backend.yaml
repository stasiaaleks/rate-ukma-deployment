apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: rate-ukma-backend
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: rate-ukma-backend
  policy:
    semver:
      range: ">=0.0.0"
