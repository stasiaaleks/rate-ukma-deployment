apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: apps
  namespace: flux-system
spec:
  interval: 5m
  # controllers (CNPG, Dragonfly, Sealed Secrets) to be ready first
  dependsOn:
    - name: infrastructure
  path: ./environments/staging
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  wait: true
  timeout: 10m
